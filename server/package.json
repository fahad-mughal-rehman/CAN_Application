{
  "name": "can-bridge-server",
  "version": "1.0.0",
  "description": "TCP bridge + HTTP/WebSocket API between the React CAN dashboard and client.cpp",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "ws": "^8.18.0"
  }
}
